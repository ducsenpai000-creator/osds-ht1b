def main():
    for _ in range(int(input())):
        gioi_han = input()
        arr = list(map(int, input().split()))
        exp = {}
        result
        for num in arr:
            if num not in exp:
                exp